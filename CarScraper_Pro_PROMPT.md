# Mega-Prompt: CarScraper Pro for Gemini (Google Colab MVP)

This prompt is designed for Gemini (1.5 Pro/Flash or 2.0 Experimental) to generate a complete, working Google Colab script for CarScraper Pro MVP.

Prompt intent
- Create a self-contained Colab notebook that scrapes car listings, analyzes each listing with Gemini, and outputs results in a clean table.

Scope
- Target site: Bazos.sk (Škoda Octavia category as example)
- Data: title, price, description, link, location, date_posted
- AI: analyze each listing and provide verdict and value
- Output: Pandas DataFrame; optionally CSV export and Telegram alert
- Security: include a placeholder for GOOG_API_KEY/GOOGLE_API_KEY

Structure
- 1) Import libraries (requests, bs4, pandas, google generative AI)
- 2) Scraping function: get_listings(search_query, min_price, max_price)
- 3) AI analysis function: analyze_listing(car_dict)
- 4) Main runner: fetch listings, analyze, collect results, display
- 5) Helper: export_to_csv(df, path)
- 6) Guard: ensure API key is provided (via Colab secrets)

Sample code outline (to be generated by Gemini)
```
import requests
from bs4 import BeautifulSoup
import pandas as pd
import google.generativeai as genai

GOOGLE_API_KEY = "<PLACEHOLDER_FOR_COLAB_SECRET>"
genai.configure(api_key=GOOGLE_API_KEY)

def get_listings(...): pass
def analyze_listing(car): pass
def main():
    listings = get_listings(...)
    results = []
    for car in listings:
        ai = analyze_listing(car)
        car.update(ai)
        results.append(car)
    df = pd.DataFrame(results)
    display(df)
    df.to_csv("car_scraper_results.csv", index=False)
```

Prompt end

Notes
- The generated code should be ready to copy-paste into a Colab cell.
- Include error handling and clear comments in Slovak.
- Gemini response should be parsable JSON if returning AI results; otherwise structured dicts.


